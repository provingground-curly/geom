# -*- python -*-

import lsst.sconsUtils

dependencies = {
    "required": ["utils", ],
}

config = lsst.sconsUtils.Configuration(
    __file__,
    headers=[], libs=[],
    hasDoxygenInclude=False,
    hasSwigFiles=False,
)
